if g:dark_colorscheme
    let g:lightline = {
                \ 'colorscheme': 'iceberg',
                \}
else
    let g:lightline = {
                \ 'colorscheme': 'iceberg',
                \}
endif
let g:lightline.subseparator = { 'left': '', 'right': '' }
